# Human actions — what no agent in this repository can do

The register of work reserved to a person. Everything here is blocked on a human **by nature**, not
by scheduling: an agent may inspect, plan, edit, test, verify and report, and may not approve, accept
risk, conclude a validation, or authorise a release — nor stand in for a party this framework's own
findings define as external.

**Why this file exists.** `activity/register.md` records work; `org/FINDINGS.md` records defects.
Neither answers the question *"what is waiting on me?"*, and the answer had been scattered across
decision records, findings entries and commit messages. Created at `ACT-039`, before an unattended
overnight run, so that what needs a human survives independently of what an agent got through.

**How to read the last column.** *Prepared* means the work an agent could legitimately do first has
been done, so the human action can start cold rather than beginning with an hour of reconstruction.

## Open

| # | Action | Why no agent can do it | Blocked behind it | Prepared |
|---|---|---|---|---|
| H26 | **Check, about quarterly, whether `pytest-textual-snapshot` has released a version permitting `syrupy >= 5`** (`F131`) | Nothing an agent can do offline, and nothing that can be automated into this repository's CI without adding an outbound call on a schedule nobody reads. It is a calendar item | Nothing. `F131` is accepted; this is the check that would let it close by remediation instead | **Your own framing, 2026-09-11:** *"if it is something I need to check periodically [that] is an action that I should have in my calendar but not a finding."* Correct, and `F131` moved to `Accepted` in the same change so it stops reading as outstanding work. **The check is one command:** `pip index versions pytest-textual-snapshot` — if it is still 1.1.0 (released 2025-01-23), nothing has changed. If a newer one appears, confirm it permits `syrupy >= 5`, then bump the chain and `F131` closes by remediation. Last checked 2026-09-11: still 1.1.0 |
| H4 | **`F6`: external recomputation of the framework digest** | Two independent reasons. `F6` names the closing party as *"someone who is not the maintainer"* — so not you either, acting alone. And the first review established the second: it must be a party who can **compute**, not only read. A text-only model cannot produce a SHA-256 | `F6`, the oldest open finding, and item 10 | **Distributed 2026-09-03 (`ACT-062`, `H16` half taken):** the [GitHub Release for tag `pypi/0.16.1`](https://github.com/pipoventures/surfaceplate/releases/tag/pypi/0.16.1) carries the page `INDEPENDENT_REVIEW_PACKET-0.16.1.html` (sha256 `59bd0a33352d…`), the archive `surfaceplate-0.16.1.zip` (sha256 `b097ca5c84d7…`) and `SHA256SUMS`, generated for the published commit `3231f3a8851cbdfa663f02bec94022ead85c73a0` (expected anchor `da83490c7fe6…`) with your approval of the exact release notes. **Nobody has been sent it yet — posting to a Part-A channel is the other half of `H16`, still open.** `audit/REVIEW_INVITATION.md` carries the covering message for both parts and three channel-specific postings, drafted for you to send. To regenerate for a later release: `.venv/bin/python scripts/build_review_packet.py --ref <commit> --sdist-url <PyPI sdist URL> --sdist-sha256 <hex> --wheel-sha256 <hex> --publish-run <id> --ci-run <id> --zip <archive>` where the archive is built from a worktree at that commit with `build_release`'s `payload_files`/`write_manifest`/`build_zip` called directly (its `main()` test gate cannot run there because `.venv`'s editable install of `surfaceplate` always resolves `from surfaceplate import about` back to the main checkout regardless of the worktree — found doing exactly this for 0.16.1; every `build_release.py` run in the main checkout replaces `dist/` with the current tree's, so copy the worktree's archive out before running it again there; the generator refuses any archive whose inner manifest is not the published one). The 0.16.0 packet at `~/surfaceplate-review-0.16.0/` (anchor `7d5b7a44…`) remains valid for that release but is not yet attached anywhere. Part A takes about thirty minutes. On return: save the YAML as `governance/assurance/AE-0002-framework-anchor.yaml`, run `tests/validate_contracts.py`, check the three values equal the anchor of the release reviewed and the reviewer is named and not you; then `F6` closes citing the record and this row moves below |
| H6 | **Item 10: independent audit** | The final gate. `org/decisions/README.md` records that no independent validator exists for this repository; an audit is what would create one. Self-approval is not independence | 1.0. It is the last item | **Distributed the same way as `H4`** — Part B of the same packet, at the same release: the full-archive prompt verbatim, one row per scope criterion, the time-boxed minimum, and a form that composes `governance/assurance/AE-0003-independent-audit.yaml` and the report as Markdown. On return: save the report verbatim as `audit/INDEPENDENT_REVIEW_<date>.md`, the record under `governance/assurance/`, record each finding as `F<n>`, and decide whether the profile's `independent_validator` names the reviewer. **`DR-65` states the standing rule this follows: verified against the code before recording, and no automated badge or attestation may substitute for it** |
| H16 | **Half taken 2026-09-03: the release is published.** What remains: **post the Part A invitation to at least the Reproducible Builds channel** | Posting to an external mailing list or forum is an outward-facing act only you can take; the text is drafted, the sending is not | `H4`, `H6` are reachable at the release, but nobody has been told it exists yet | The [release](https://github.com/pipoventures/surfaceplate/releases/tag/pypi/0.16.1) is live with all three assets, verified against `SHA256SUMS`, published only after you approved the exact notes in-session. `audit/REVIEW_INVITATION.md` has the ready-to-send text for the Reproducible Builds list, `discuss.python.org`, and Show HN — pick one and send it. **Noted 2026-09-08:** your stated intended route is a named reviewer engaged through PwC, not these three channels — not yet initiated, no packet sent. Still **pending**; the `H19` clock has not started by either route. **And the packet itself is now superseded, which changes what this action is.** The distributed packet is built from the published `0.16.1` commit; `ACT-068`/`ACT-069` have since replaced the entire document corpus with the twelve-topic axis, added `adopter_canon` and `SP060`, and implemented `DR-1`. Your own 2026-09-05 ruling gated the restructure on *"not sending the review packet against the pre-restructure corpus"* — the restructure has now happened, so sending the `0.16.1` packet is the thing that ruling was written to prevent. **The order is therefore: publish the accumulated version, regenerate the packet from that published commit (`scripts/build_review_packet.py`, whose generator refuses an archive whose inner manifest is not the published anchor), then send.** Part A's anchor recomputation remains valid per release, so nothing already done is wasted — but a reviewer given the old packet would audit a structure that no longer exists |
| H18 | **Watch three to five people install Surfaceplate on a screen share, saying nothing, and record what they hesitate on** | Only the maintainer can convene this; an agent cannot recruit or observe a live human session | The strongest available signal for silent adopter friction, per this session's recommendation | **Route decided 2026-09-08:** it happens as part of the PwC integration/testing, not as a separately convened session. That makes it downstream of `H16`'s PwC engagement rather than independent of it — the observation is only as good as the corpus the observers install, so it waits for the published post-restructure version the same way the review packet does. Not started. Candidates come from that engagement **`F172` adds one thing to watch for:** whether anyone using neither Claude Code nor Copilot - Cursor, Zed, Aider, Codex - balks at being made to carry one of the two vendor directories anyway. `AGENTS.md` and `.standards/topics/` already serve them, and `DR-67` refuses an empty channel list deliberately; this is the only instrument that would turn that constraint from a guess into a fact. |

## Standing practices, not one-off actions

| # | Practice | Why it is here |
|---|---|---|
| S1 | **`F55`: verify a docstring's claim when touching the code beneath it** | Open with no mechanical remedy. Two docstrings were found wrong about their own code on 2026-09-01, both corrected. A comment describing a guarantee is a claim to be checked, not a fact |
| S3 | **Run an instruction before publishing it** | `F50` and `F57` are the same defect one layer apart: a hand-off command naming a file deleted three packets earlier, and a README, an `INSTALL.md` and the tool itself naming a package that 404s. Both survived because **a document is read for sense rather than run**. `F50`'s half is now checked by `tests/check_audit_packet.py`; **`F57`'s is not, and deliberately so** — whether `pip install X` resolves cannot be answered offline, and a check that would not have caught the defect it is named for is the false green this project exists to find |
| S2 | **Approve, or decline, each merge** | An agent merges on your authorisation and never on its own judgement. Where a packet was planned and approved, the merge is mechanical; where it was not, it waits |

## Closed

| # | Action | Closed |
|---|---|---|
| H27 | Repair this machine's global git configuration, which `scripts/front_door.sh` overwrote (`F164`) | 2026-09-11 — **done by the maintainer, and verified here by effect rather than taken on trust.** `git config --show-origin --get-all core.hooksPath` now returns only `surfaceplate`'s own local `hooks-chain`; a freshly initialised repository elsewhere reports no hooks path at all, so `.git/hooks` is live again everywhere. Global identity is `mps2210` with the maintainer's own address — **not the one this row proposed.** It had named the address read back from pre-9-September history; history is evidence of what was used, not of what is wanted, and that gap is exactly why the row asked him to check rather than asking an agent to apply it. The address itself is deliberately not reproduced here: this register is public, and recording that the maintainer set his own value carries the whole of what a reader needs. The 172 commits already authored as "A stranger" on `main` in `plyego` and `actually-using-ai` are left as they are |
| H25 | Decide whether the history audit should exclude commits made in the same second as `effective_from` (`F136`'s `PW-18`) | 2026-09-11 — **the window opens at the first second Git can distinguish from the declaration.** `ACT-096` implements it, `F158` closes. What decided it was which error the adopter can act on: a commit that predates adoption cannot be rewritten, so their only remedy was a gate exception for a commit that did nothing wrong — a ceremony that teaches people to record exceptions for non-events. The cost is one second of silence after the gate takes effect. **The agent's own first costing of this was wrong and was corrected before the decision:** it offered an "exact" route comparing timestamps after `git log` returns, and Git stores commit times as whole seconds, so no such filter exists |
| H24 | Decide whether an ordinary (non-chained) install should claim `local_hook` (`F156`) | 2026-09-11 — **yes, but only after fixing the check first**, which is the sequencing the maintainer chose over two alternatives. `DR-84` records it, `ACT-096` implements it, `F157` closes. Investigating the question found that claiming `local_hook` would have failed EVERY claiming adopter's CI 30 days after install — `SP038` is graceable and a fresh clone has no hook by construction — so the answer to the question as asked was no, and the answer worth having was to make `SP038` report only what it can establish (`DR-74`) and then claim it for everybody. It also caught a regression `ACT-095` had shipped the day before |
| H23 | Decide how an upgrade re-pins `framework_version` and `framework_digest` (`F146`) | 2026-09-09 — **route (2)**: an explicit `surfaceplate adopt --repin`, not a silent installer re-pin. `DR-81` records it, `ACT-092` implements it, `F146` closes. The reasoning that decided it: `DR-45` reads the digest as the ADOPTER's claim about the distribution they assessed against, so the deliberate act must remain theirs — while hand-copying a 64-character digest was never a claim about anything, and is what the command removes |
| H22 | Decide how `dependency_lock` applies to a repository that has no dependencies (`F132`) | 2026-09-09 — **route (1) chosen**: derive applicability from the absence of any dependency manifest and check it, so nothing is declared and nothing can be misdeclared. `DR-73` records it, `ACT-078` implements it, `F132` closes. Route (2), a declared `not_applicable` verified by the checker, was rejected on the narrow ground that there is nothing here for a human to contribute — whether a repository has a manifest is a fact about their tree, not a judgement about their risk, and a declaration that adds no information adds only ways to be wrong |
| H21 | Decide what to do about `CVE-2025-71176` in `pytest`, which has no satisfiable upgrade (`F131`) | 2026-09-08 — **route (1), accept and defer**: *"Agree."* The residual risk is accepted on the recorded reading — a test-only extra no adopter installs, and a local attack needing an unprivileged user racing `/tmp/pytest-of-{user}` on an ephemeral runner or the maintainer's own workstation. **`F131` stays open**: an accepted risk and a fixed defect are different states. Review trigger: a `pytest-textual-snapshot` release permitting `syrupy >= 5`. The alert itself is dismissed in GitHub by the maintainer — an agent's attempt to silence a security alert was refused by a permission classifier, which is the correct outcome and is recorded rather than worked around |
| H20 | Decide whether the installed conformance workflow calls `pypi.org` on every run (`F124`'s remaining half) | 2026-09-08 — **option (b) chosen**: on by default, with an opt-out key in the adopter's profile. `DR-72` records it and `ACT-075` implements it. The reasoning that decided it: the installed workflow is integrity-checked, so an adopter cannot delete the step — which made (a) *compulsory* rather than merely *default*, and made the profile the only place a switch could honestly live. `F124`, the highest-severity finding this framework had raised against itself, closes with it |
| H17 | Enable private vulnerability reporting (`F118`) and enable Discussions | 2026-09-08 — **both now on, and one of them turned out already to have been.** Private vulnerability reporting answered `{"enabled":true}` when queried (`gh api repos/pipoventures/surfaceplate/private-vulnerability-reporting`) — enabled at some point without `SECURITY.md` being updated, so the file's *"is not enabled today"* paragraph had gone stale a second time, in the same file and about the same feature as `F118`. Discussions were `false` and were enabled in-session by the agent (`gh api -X PATCH repos/pipoventures/surfaceplate -F has_discussions=true` → `{"has_discussions":true}`) — a repository setting, not an approval, which is why an agent could take it. `SECURITY.md` rewritten in place to state the verified current fact and to mark the two stale readings as history |
| H19 | Decide on paid independent review (~£200–500) if the free channels have not produced a Part A response | 2026-09-08 — decided in the negative, standing rather than deferred: *"Never paid review, I'll do it on free channels and through PwC."* No spend is contemplated at any point, so this is not a clock waiting to start — the question is closed. `H4` and `H6` therefore depend entirely on the free channels and the PwC route, with no paid fallback behind them; that is now a known property of the plan rather than an option held in reserve |
| H9 | Take decisions `DR-47` to `DR-50` and authorise `ACT-042` to `ACT-046` | 2026-09-02 — all four records accepted as drafted, all five activities authorised, findings `F59` to `F77` recorded. Filed under Open until 2026-09-08 as "the evidence reference the records cite"; that reference is preserved here, where a taken action belongs |
| H11 | Decide the remedy for `F78` to `F82` — what the wizard shows a reader meeting the framework for the first time | 2026-09-02 — the maintainer instructed *"Fix findings 78 to 82. Then fix findings SP046 and SP032"* after his own run; `DR-51` records the decision and `ACT-048` implements it |
| H12 | Set the outcome of the `adopt` validation record (`AE-0001-adopt-matrix.yaml`) | 2026-09-02 — PR #60 merged at `bc2fd94` with the record as drafted, `passed_with_conditions`, the maintainer named as reviewer; the nine limitations stand as the conditions |
| — | Decide publication (`ACT-007`) | 2026-09-01, on judgement, with the unverifiable residue recorded as `F49` |
| H14 | Ratify or delete the release tags `pypi/0.16.0` and `pypi/0.16.1` (`F115`) | 2026-09-03 — the maintainer ratified both (*"Ratified"*, this session: https://claude.ai/code/session_01Bz6QZWcsg9tRFuH9gS331Z). They stand as the tags that name published commits; `v0.16.0` stays where it was. `F115` closed |
| H5 | `F49`: search the tree for the former organisation's token | 2026-09-02 — the maintainer ran the one command with the name in place: zero hits on the tracked tree at `main`. A first attempt searched the placeholder itself and returned the file quoting the command; the second closed it. `F49` closed |
| H10 | Verify the organisation-level GitHub Actions claim and say which document wins | 2026-09-02 — the maintainer showed the organisation policy: Actions enabled for all repositories, all actions allowed. The README wins; `PREREQUISITE_GATES.md`'s paragraph rewritten; `F71` closed |
| H7 | Decide forge neutrality (item 3's remainder) | 2026-09-02 — `DR-62`: 1.0 supports GitHub as the forge and says so in `README.md` and `INSTALL.md`; a per-forge emitter is 1.x (`1.4`). Item 3 done with its scope stated |
| H3 | Second cross-provider adversarial review (item 9, pass 2) | 2026-09-02 — run by the maintainer with the curated prompt and the 27-file bundle from `main` at `56d2163`; the review reproduced verbatim as `audit/CROSS_PROVIDER_REVIEW_2026-09-02_PASS2.md` and its eleven points recorded as `F101` to `F111`, each assessed. The digest recomputation was an evidence gap (no code execution), so `H4` stays open |
| H13 | Decide the remedies for the second cross-provider review (`F101` to `F111`) | 2026-09-02/03 — every item put one at a time and decided: `F107` fixed as proposed; `F106`, `F109`, `F110` profile edits approved; `F108` note omitted; `F101` rollback; `F103` acceptance line; `F102` seed advisory over the reviewer's placeholder; `F104`/`F105` schema tightened (`DR-63`); `F111` the practice kept. `ACT-059` done |
| H8 | Reserve the name on PyPI (`DR-61`) | 2026-09-03 — the maintainer created the account and the pending trusted publisher, the `pypi` environment was created, and he dispatched *Publish to PyPI* (run 33697386488, both jobs green). `surfaceplate 0.16.0` is on the index with the alpha classifier, a wheel and an sdist; installed from PyPI into a clean environment and `surfaceplate --version` answered. Every instruction keeps the git form until 1.0 |
| — | Choose the `F47` remedy | 2026-09-01 — `effective_from` accepts an instant (`DR-44`) |
| — | Choose the `F51` remedy | 2026-09-02 — asked again, and the binding rule made precise (`DR-46`) |
| — | Merge or reject the README front-door PR | 2026-09-02 — merged as PR #39 on your authorisation |
| H1 | Run `adopt` against Plutos (`RELEASE_PLAN` item 5) | 2026-09-02 — run by the maintainer on the rebuilt wizard; stopped once at the review on a version mismatch (`F78`), resumed after the upgrade, and completed; the two checker findings it left (`F83`, `F84`) fixed by hand and merged as plutos#6, after which `surfaceplate check` passes there in full. Eight findings (`F78` to `F85`) recorded from the run and closed by `ACT-048` |
| H2 | Exercise or adopt Plyego (`RELEASE_PLAN` item 5) | 2026-09-02 — closed by `DR-52`: item 5 is met with one real adopter, Plutos; Plyego is mid-migration with important work ahead and is not to be touched. `DR-28` stands as the record of its exercise |
| — | First cross-provider review (item 9, pass 1) | 2026-09-01, returned `FAIL`; acted on at `ACT-038`. **It did not narrow `F6`** — see H4 |

## What an agent may not do here, restated

Recorded so this file is not read as a to-do list an agent could simply work through given more
time. None of the above is blocked on effort:

- **approve, accept risk, conclude a validation, or authorise a release** — never an agent's,
  however clear the intent and authority appear;
- **stand in for an external party** — `F6`'s closing condition and item 10 both define the closer as
  someone other than this repository, and an agent inside it is the least independent party of all;
- **decide methodology, a control's semantics, or what the framework ships** — H7 is the live
  example;
- **supply a value the maintainer holds and the records deliberately do not** — H5.
